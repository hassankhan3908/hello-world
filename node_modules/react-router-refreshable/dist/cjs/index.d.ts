import { PropsWithChildren } from 'react';
export declare function Refreshable({ children, on }: PropsWithChildren<{
    on?: () => void;
}>): JSX.Element;
export declare function useIsRefreshingRef(): {
    current: boolean;
};
